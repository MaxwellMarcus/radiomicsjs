import Mesh from '../core/Mesh.js';
interface Input {
    operation: 'readMesh' | 'writeMesh';
    config: {
        meshIOUrl: string;
    };
}
export interface ReadMeshInput extends Input {
    name: string;
    type: string;
    data: ArrayBuffer;
}
export interface WriteMeshInput extends Input {
    name: string;
    type: string;
    mesh: Mesh;
    useCompression: boolean;
    binaryFileType: boolean;
}
export declare function readMesh(input: ReadMeshInput): Promise<any>;
export declare function writeMesh(input: WriteMeshInput): Promise<any>;
export {};
